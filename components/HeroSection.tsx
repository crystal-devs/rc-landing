'use client';

import AnimatedPhotoWall from './AnimatedPhotoWall';

export default function HeroSection() {
  // AnimatedPhotoWall now contains both hero and grid sections
  // with images that move from scattered to grid positions
  return <AnimatedPhotoWall />;
}